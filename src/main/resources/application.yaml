spring:
  jpa:
    hibernate.ddl-auto: validate
    show-sql: true
logging:
  level:
    org:
      springframework:
        jdbc:
          datasource:
            init:
              ScriptUtils: debug